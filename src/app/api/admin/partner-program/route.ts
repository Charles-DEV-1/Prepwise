import { NextRequest, NextResponse } from "next/server";
import { forbiddenResponse, getAdminSessionUser } from "@/lib/admin-api-auth";
import { createServiceRoleClient } from "@/services/supabase/admin";
export async function GET(){if(!(await getAdminSessionUser()))return forbiddenResponse();const {data,error}=await createServiceRoleClient().from("partner_accounts").select("id,full_name,email,phone,business_name,city,partner_type,referral_code,status,total_earned,pending_balance,created_at").order("created_at",{ascending:false});if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({partners:data??[]});}
export async function PATCH(request:NextRequest){const adminUser=await getAdminSessionUser();if(!adminUser)return forbiddenResponse();const {id,status}=await request.json().catch(()=>({}));if(!id||!["active","suspended"].includes(status))return NextResponse.json({error:"Invalid request"},{status:400});const {error}=await createServiceRoleClient().from("partner_accounts").update({status,approved_at:status==="active"?new Date().toISOString():null,approved_by:adminUser.email} as never).eq("id",String(id));if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({success:true});}
